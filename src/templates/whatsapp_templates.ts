const token = (token:number|string) => `Utilize o seguinte token para confirmar sua assinatura: ${token}`

const confirmacao = `Olá, sua assinatura foi confirmada. Obrigado!`

export default {token, confirmacao}